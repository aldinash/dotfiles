# shellcheck shell=bash

[[ $- == *i* ]] || return 0

# shellcheck source=/dev/null
[[ -f "$HOME/.config/dotfiles/shell/common.sh" ]] &&
  source "$HOME/.config/dotfiles/shell/common.sh"
# shellcheck source=/dev/null
[[ -f "$HOME/.config/dotfiles/shell/bash.sh" ]] &&
  source "$HOME/.config/dotfiles/shell/bash.sh"
